include ../view-modules/modules

doctype
html
    +mod-html-head("Refund & Cancellation Policy")
    body.bg--grey--alpha
        +mod-social-icons-sticky()
        +mod-header(navItems, '')
        +mod-refund-policy()
        +mod-footer(footerItems)
        +mod-html-scripts()